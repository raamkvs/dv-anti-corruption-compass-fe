import MethodologyPage from './index';

export function MobileMethodologyPage() {
  // Reuse the same content; typography and spacing are adjusted via `.mobileApp` styles.
  return <MethodologyPage />;
}
